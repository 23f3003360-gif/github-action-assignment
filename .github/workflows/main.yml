name: Assignment Workflow

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # Allows manual triggering

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: 23f3003360@ds.study.iitm.ac.in
        run: echo "Hello, this step is named with my email!"
      
      - name: Another step
        run: echo "This is just an extra step"
